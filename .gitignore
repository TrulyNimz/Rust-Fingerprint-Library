# Rust build output
target/
**/*.rs.bk
Cargo.lock.bak

# Node
node_modules/
npm-debug.log

# napi-rs binary artifacts (platform-specific)
*.node

# MSVC/linker leftovers (from building the sgwsqlib stub in bridge/)
*.pdb
*.obj
*.exp
*.lib
*.rcgu.o

# Built native binaries — generated by build commands, not source
bridge/sgwsqlib.dll
sdk/secugen-bridge.exe

# Vendor SDK blobs (large, license-restricted, fetched separately)
neurotechnology-sdk/
neurotechnology-sdk.zip
*.exe

# Editor / IDE / tooling
.vscode/
.idea/
.claude/
*.swp
.DS_Store
